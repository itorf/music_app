<h1>Create A New Band</h1>

<form action="<%= bands_url %>" method="POST">
	<input type ="hidden" 
				name="authenticity_token" 
				value="<%= form_authenticity_token %>">
				
	<label for="band_id">Band Name</label>
	<input type="text" name="band[name]" value="<%= @band.name %>" id="band_id">
</br>
	<input type="submit" value="Create This Band">
</form>
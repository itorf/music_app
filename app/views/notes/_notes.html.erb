<% track.notes.each do |note| %>
	<%= note.body %></br>
	Posted by: <%= note.user.email %></br>
	<% if current_user.id == note.user_id %>
		<%= button_to "Delete Note", note_url(note), 
		method: :delete %>
	<% end %></br>
<% end %>